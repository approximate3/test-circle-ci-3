version: 2.1
jobs:
  mocha:
    docker:
      - image: cimg/node:18.10.0
    steps:
      - checkout
      - run: npm install
      - run: npm run wdio

workflows:
  version: 2.1
  build:
    jobs:
      - mocha
